services:
  n8n:
    build: .
    ports:
      - "5678:5678"
    env_file:
      - .env
    environment:
      WEBHOOK_URL: ${N8N_WEBHOOK_URL}
      EXECUTIONS_DATA_PRUNE: "true"
    volumes:
      - n8n_data:/home/node/.n8n
      - ./tmp:/tmp/audio-brief

volumes:
  n8n_data:
